# Arrays & Strings
This project contains solutions for array and string problems in C#.

## Problems
1. **Reverse String**: Reverse a character array in-place.
2. **Two Sum**: Find two numbers in an array that sum to a target.

## How to Run
1. Navigate to `ArraysAndStrings` folder.
2. Run `dotnet run` to execute `Program.cs`.