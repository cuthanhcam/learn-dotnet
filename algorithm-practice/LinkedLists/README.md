# Linked Lists
This project contains solutions for linked list problems in C#.

## Problems
1. **Reverse Linked List**: Reverse a singly linked list.
2. **Merge Two Sorted Lists**: Merge two sorted linked lists into one sorted list.

## How to Run
1. Navigate to `LinkedLists` folder.
2. Run `dotnet run` to execute `Program.cs`.